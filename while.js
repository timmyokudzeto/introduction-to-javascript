// Set population limit of aquarium to 10

// Start off with 0 fish


// Initiate while loop to run until fish reaches population limit

    // add one fish for each iteration



const popLimit = 10;
let fish = 0;
while (fish < popLimit) {
    fish++;
      console.log("There's room for " + (popLimit - fish) + " more fish.");
      }
      25 people re waiting to ener shoprit and they are clodsed for the day only 15 people can enter




const people = 15;
var allowed = 0;
      while (allowed < people) {
        allowed++
      alert("there is room left for only " + (people - allowed) + " more fish");
      }
